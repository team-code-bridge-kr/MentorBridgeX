# WebSocket 업그레이드용 Connection 헤더 매핑 (STT 실시간 /v1/stt)
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # 오디오 업로드(STT) 여유
    client_max_body_size 64m;

    # API 프록시 — 프론트는 same-origin 으로 /v1/** 을 호출한다.
    # ^~ 로 아래 정적파일 정규식 location 보다 우선하게 한다.
    location ^~ /v1/ {
        proxy_pass http://api:8000;
        proxy_http_version 1.1;

        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket (STT 실시간)
        proxy_set_header Upgrade    $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # LLM/STT 응답이 길어질 수 있어 타임아웃을 늘린다
        proxy_connect_timeout 10s;
        proxy_send_timeout    300s;
        proxy_read_timeout    300s;
        proxy_buffering       off;
    }

    location = /health {
        proxy_pass http://api:8000/health;
        proxy_set_header Host $host;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 껍데기(index.html)는 캐시하지 않는다.
    # 안에 실린 js/css 는 이름에 해시가 박혀 있어 7일을 캐시해도 안전하지만,
    # 그 이름을 가리키는 것이 이 파일이다. Cache-Control 을 안 주면 브라우저가
    # 제 나름의 셈으로 붙들고 있어서, 새로 올린 화면이 안 보이는 일이 생긴다.
    # no-cache 는 "쓰지 마라" 가 아니라 "쓰기 전에 물어봐라" 다 — 안 바뀌었으면
    # 304 로 끝나므로 값이 거의 들지 않는다.
    location = /index.html {
        add_header Cache-Control "no-cache";
    }

    # .mjs 는 기본 mime.types 에 없어서 application/octet-stream 으로 나가고,
    # 그러면 브라우저가 모듈 스크립트로 받지 않는다 (pdf.js 워커가 여기서 막혔다).
    # server 수준 types 블록은 상속받은 mime.types 를 통째로 덮어써서 HTML 까지
    # 깨뜨리므로, 이 확장자에만 default_type 을 준다.
    location ~* \.mjs$ {
        try_files $uri =404;
        default_type application/javascript;
        expires 7d;
        add_header Cache-Control "public";
    }

    location ~* \.(?:js|css|svg|png|jpg|jpeg|gif|ico|woff2?)$ {
        try_files $uri =404;
        expires 7d;
        add_header Cache-Control "public";
    }
}
